import org.junit.Test;

public class TestPlayer {

    @Test
    public void testDeployment() {
        //Player p1 = new Player(10, 1, Color.color(20, 20, 20));
        //Player p2 = new Player(10, 2, Color.color(100, 0, 20));

        //Deployment dp = new Deployment();
        //dp.startDeployment(p1, 5);


    }
}
